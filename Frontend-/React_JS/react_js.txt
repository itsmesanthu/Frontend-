            module 1
        (indtroduction to react)
• HTML (Hypertext Markup Language)
    Basic Structure: HTML builds the skeleton of web pages by using tags to define elements like text, images, and links.

• CSS (Cascading Style Sheets)
    Design and Style: CSS controls how web pages look, allowing you to change colors, fonts, and layouts to make the site attractive.

• JavaScript
    Interactive Features: JavaScript makes web pages interactive, letting users do things like click buttons, fill out forms, and see changes without refreshing the page.

What is React JS?
    • React JS is an open-source JavaScript library developed and maintained by Facebook.
    • It’s widely used for building user interfaces, especially for single-page applications and complex web interfaces.
    • It breaks down a whole website into small, manageable pieces called components.
    • It enables developers to create dynamic, responsive, and reusable UI components, enhancing the efficiency of the development process.
Why React JS?
    • Speed: React uses the Virtual DOM, allowing efficient updates and rendering, resulting in faster and more responsive user interfaces.
    • Reusability: React follows a component-based approach where components can be reused. This leads to cleaner code, easier maintenance, and better collaboration among team members.
    • Job Market: React has high demand. Companies like Netflix, Airbnb, Instagram, etc. use React.
    • Rich Ecosystem and Tooling: React is supported by a vast ecosystem of libraries, tools, and frameworks that enhance its capabilities and help developers build robust applications more efficiently.
About React JS
    • It is a JavaScript library developed by Facebook in 2011. It has been released in 2013.
    • It has been invented by Jordan Walke.
    • Angular JS and Vue JS are the competitors for React.
    • It has Component-Based Architecture, where the component can be reusable which helps in maintaining and organizing the code efficiently.
    • React uses a Virtual DOM to optimize rendering, which improves performance.
    • Unidirectional Data Flow: It means data flows in a single direction, from parent components to child components.
Single Page Application (SPA)
    •⁠  ⁠A Single Page Application (SPA) in React is a web application where the entire website loads only once in the browser. After that, different pages or content are updated dynamically without refreshing the whole page.
What is the purpose of React JS?
• React is used to create user interfaces.
• React is used to create single-page applications with other libraries.
• Using React, we can create static and dynamic web pages.
Can we create an entire Frontend application using React?
    To create frontend application with React?
    UI + Other Libraries
    Other Libraries:
        Axios
        Hooks
        React Router DOM
        React Icons
        Redux
    React is mainly used for building the UI (User Interface).
    To create a complete frontend application, React is combined with other libraries for routing, API calls, state management, and icons.
Advantages of React JS
    • React is easy to learn and use.
    • React learning curve is very small.
    • React applications are very fast in performance.
    • In React, we have Virtual DOM.
    • React has component-based architecture.
    • React community is large.
Difference between Angular JS and React JS
|                |    Angular JS   |   React JS      |
| -------------- | --------------- | --------------- |
| Author         | Google          | Facebook        |
| Developer      | Misko Hevery    | Jordon Walks    |
| Latest Version | 16.0.3          | 19.0.0          | 
| Language       | JS, HTML        | JSX             |
| Rendering      | Client Side     | Server Side     |
| Data Binding   | Bi Directional  | Uni Directional |
| Dom            | Regular DOM     | Virtual DOM     |
|AppArchitecture | MVC             | Flux            |
| Routing        | Manual          | Modules Used    |
Difference Between React JS and React Native
*React JS* 
    Released in 2013 
    For Web Application
    Executed on all Platforms
    Uses JS and CSS lib for Animations
    React router for navigating Web Pages
    Uses HTML Tags
    High Security
*React Native*
    Released in 2015
    For Mobile Application
    Platform Independent
    Built in Animations
    Built  in Navigator Library
    Doesn’t
    Lower Security Comparatively

Working of React Application
    - If we want to navigate from one page to another page in react the application will not send any request to server
    - The page will not refresh in react. Without refreshing it will navigate from one page to another page
    - We will get request from react and react only take care of all request
    - It is also called as single page application.
    - Because it has the concept called virtual dom. let's see dom interaction

What is DOM?
    - DOM(Document Object Model) is a tree like structure that represents your HTML elements in the browser
    - This becomes a DOM tree like
    - Changing or updating this structure directly (called real DOM manipulation) is slow especially for big applications
working od react application:

+--------------------+                      +----------------------+
|      CLIENT        |                      |        SERVER        |
|--------------------|                      |----------------------|
|      Browser       | ---- HTTP Request -->|   React App Hosted   |
|  www.facebook.com  |                      |                      |
|                    |<--- HTTP Response ---|   JS Files / Data    |
+--------------------+                      +----------------------+

          CLIENT SIDE                          SERVER SIDE

        Bundles of JS files get downloaded in the browser
    - If we want to navigate from one page to another page in react the application will not send any request to server
    - The page will not refresh in react. Without refreshing it will navigate from one page to another page.
    - We will get request from react and react only take care of all request
    - It is also called as single page application.
    - Because it has the concept called virtual dom. let's see dom interaction.

What is DOM?
    - DOM(Document Object Model) is a tree like structure that represents your HTML elements in the browser
    - This becomes a DOM tree like
    - Changing or updating this structure directly (called real DOM manipulation) is slow especially for big applications

 What is the Virtual DOM?
    - The Virtual DOM is a copy of the real DOM kept in memory by react.
    - It's a JavaScript object that mirrors the structure of the real DOM. But doesn't touch the screen immediately.

React never updates the DOM directly. Instead it
    - Creates a virtual version of the Ul
    - Updates that virtual version when state or props change
    - Compares it with the previous version
    - Finds what changed
    - Efficiently updates only those parts in the real DOM.

When is the virtual DOM used?
    •⁠  ⁠Every time something changes in your app (like a button click or input text change) React.
    +------------------+        +------------------+        +------------------+
    | User Interaction | -----> |   Virtual DOM    | -----> |   Original DOM   |
    |------------------|        |------------------|        |------------------|
    |      Client      |        | React compares   |        | Real webpage     |
    | User clicks/type |        | changes here     |        | gets updated     |
    +------------------+        +------------------+        +------------------+

    Flow:
    User Action --> Virtual DOM --> Original DOM Update
    •⁠  ⁠Re-renders the component to create a new Virtual DOM
    •⁠  ⁠React compares the new virtual DOM with the old virtual DOM to find what changed
    •⁠  ⁠Calculates the minimum number of changes required
    •⁠  ⁠Applies those changes to the real DOM

Why is this important?
Updating the real DOM is slow because
    - It involves recalculating styles, layouts and reflows
    - The entire page may be re-rendered unnecessarily

React's virtual DOM makes updates faster and smoother by
    - Avoiding unnecessary work
    - Making Dom updates batched and optimized

+---------------------------+      +---------------------------+
|        VIRTUAL DOM        |      |        ORIGINAL DOM       |
|---------------------------|      |---------------------------|
| Document                  |      | Document                  |
|   └── HTML                |      |   └── HTML                |
|       ├── Head            |      |       ├── Head            |
|       │   ├── Title       |      |       │   ├── Title       |
|       │   ├── CSS         |      |       │   ├── CSS         |
|       │   └── JS          |      |       │   └── JS          |
|       └── Body            |      |       └── Body            |
|           ├── H1          |      |           ├── H2          |
|           ├── p           |      |           ├── p           |
|           └── button      |      |           └── button      |
+---------------------------+      +---------------------------+

React Creates a virtual Dom in memory
    •⁠  ⁠Instead of a manipulating the browser's Dom directly, React creates a virtual Dom in memory. Where it does all the necessary manipulating before making the changes in the Browser dom.
    React Changes only what need to be change in original dom
    •⁠  ⁠when user wants to change anything it will reflect in virtual dom The virtual dom will update the changes which is need in original dom.
    This entire process is Reconciliation Process in React JS
Reconciliation Process in React JS
                RECONCILIATION

   OLD Virtual DOM            NEW Virtual DOM
          |                          |
          |------ Compare ---------->|
                    (DIFF)
                         |
                         v
                 Changed Nodes Found
                         |
                         v
                  Update Real DOM



    Example:

    OLD VDOM                     NEW VDOM
    H1                           H2
        |                            |
    React compares both using DIFF algorithm
    and updates only the changed part in Real DOM.
    -The reconciliation process in React is the mechanism by which React determines what changes need to be made to the Real DOM in response to updates in the Virtual DOM.
    -It involves comparing the current Virtual DOM representation of a component with its previous representation to identify the differences and efficiently update the Real DOM.
Here's a step-by-step overview of the reconciliation process in React:
    Component Render:
        • When a component's state or props change, React re-renders the component and its children to generate a new Virtual DOM representation.
    Diffing Algorithm:
        • React performs a “diffing” or comparison process between the new Virtual DOM tree and the previous Virtual DOM tree for the component.
        • It recursively traverses the component tree and compares each node (element) in the new tree with its counterpart in the previous tree.
Identifying Changes:
    React identifies three types of changes:
        • Added Nodes
        • Removed Nodes
        • Updated Nodes
    Applying Changes:
        • React efficiently applies the identified changes to the Real DOM, updating only the affected parts without re-rendering the entire DOM tree.
        • This process involves adding, removing, or updating DOM elements and attributes as necessary to reflect the changes in the UI.

By performing this reconciliation process, React ensures that the UI remains in sync with the application state while minimizing unnecessary updates to the Real DOM, leading to better performance and a smoother user experience.

Difference b/w Real DOM and Virtual DOM

    | Real DOM                                           | Virtual DOM                                                          |
    | -------------------------------------------------- | -------------------------------------------------------------------- |
    | Represents the actual structure of a web page.     | A lightweight, in-memory representation.                             |
    | Created by the browser when a page loads.          | Created and maintained by React for each component.                  |
    | Inefficient for frequent updates.                  | Efficient for frequent updates.                                      |
    | Updating is slow and resource-intensive.           | Updating is fast and less resource-intensive.                        |
    | Directly manipulates the browser’s display.        | Indirectly manipulates the browser’s display through reconciliation. |
    | Requires re-rendering the entire tree for updates. | Requires minimal updates for changes.                                |
Features of React JS
    1. JSX
        JSX (JavaScript XML) is a syntax extension for JavaScript used in react.
        it allows you to write HTML- like code in your JavaScript components.
    2. component
        React JS is all about Component ,react jsd application  is made up of multiple components and each component has its own logic and controls.
        those component can be reusable.
    3. one-way data Binding
        React Js is designed in such a manner that follows Unidirectional data flow or one way data Binding.
        the benefits of one ways data Binding give you better controls throughout the application.
    4. virtual DOM
        A virtual DOm is a copy of original dom,wherever any modification happen in the web application the entire UI is re-rendered in virtual DOM representation.
        then it checks the defference between dom representation and new dom only it update if it need.
    5. simplicity
        React Js uses JSx file which make the application simple and to code as well as understand.
        we know that react Js is a component based approach which make the code reusable as yours need.
    6. performance
        react js is knowto be a great performer
        this Features makes it muck better then other frameworks out  there today. the representation behind this is that that it manage a virtual DOM.
why react is used to create ui when we can  create ui HTML and JS?
HTML
        UI-> HMTL-> pre defined elements
        using html creating UI is very easy .
        to create UI html is providing some html elements and tags.
    drawbacks
        we can't resuse the ui
        time consuming to create ui.
JavaScript
        UI-> JavaScript-> predefind function -> dom element->Dom->screen
        JavaScript we can create UI dynamically.
        we can re use UI
    drawbacks
        creating UI is very complex process.
        time consuming.
React js
        UI-> react-> predefind function -> react element->dom->screen


| HTML                    | JavaScript                           | React JS                                        |
| ----------------------- | ------------------------------------ | ----------------------------------------------- |
| Creating the UI is easy | We can reuse the UI                  | Creating UI is very easy for large applications |
| We cannot reuse the UI  | Creating the UI is a complex process | We can reuse the UI like JavaScript             |

Step 1: Create React Project using Vite
        npm create vite@latest

Step 2: Install create-vite package
        Type y and press Enter
        Need to install the following packages:
        create-vite@9.0.7
        Ok to proceed? (y) y

Step 3: Enter Project Name
        Project name:
        firstproject

Step 4: Select Framework
        Select a framework:
        React

Step 5: Select Variant
        Select a variant:
        JavaScript

Step 6: Install Dependencies
        Install with npm and start now?
        Yes
        Project Created Successfully
        Scaffolding project in:
        firstproject

Step 7: Move into Project Folder
        cd firstproject

Step 8: Run React Project
        npm run dev

Output
        VITE v8.0.14 ready

    Local:
    http://localhost:5173/

    Open this URL in Browser
    http://localhost:5173/

            module 2
        JSX(JavaScript xml)   
What is JSX?
    - Jsx stands for **javascript xml**.
    - It is a syntax extension for javascript commonly used in react.
    - It looks similar to html. But it allows you to write html like elements directly with in javascript.
    - Jsx is not valid javascript but it's transformed into regular javascript by tools/compiler like babel before being executed by the browser.
    - Let see the example
    let element <h1>Welcome to React JS</h1>

What is Babel Complier?
    - Babel is a JavaScript compiler (also called a transpiler) that helps you write modern JavaScript (ES6+)
    - Babel is essential for React because:
    1. React uses JSX (HTML inside JavaScript), which browsers don't understand.
    2. Babel converts JSX into regular JavaScript (e.g., React.createElement).

Rules of write JSX
    - JSX codes should be always written in lower case.
    - JSX must return a single parent element.
    - The parent should be one for the all the elements such as <div>,<section>, <main>
    - We can enclose the jsx code inside the react fragment
    - <React Fragment> </React Fragment> or <> </>. To avoid extra div error
    - React Fragments provide a way to group multiple React elements without adding extra nodes to the DOM. 
    They allow you to return multiple elements from a component's render method without needing to wrap 
    them in a single parent element.

React Fragments
    - React Fragments provide a way to group multiple React elements without adding extra nodes to the DOM.
    - They allow you to return multiple elements from a component's render method without needing to wrap them in a single parent element.
- They allow you to return multiple elements from a component's render method without needing to wrap them in a single parent element.
    - JSX attributes use camel case. Html attributes like class and onclick must be written in camel case in jsx
for example:

                    class becomes className

                    onclick becomes onClick

- Javascript expression in jsx must be enclosed in curly bracket .
- JSX tags can have multiple children we can nest elements inside jsx tags and they will render as part of tree structure
- JSX supports comments in React.   ==⇒   {/*                *}

                    MODULE-3
            (Components in react)
What are Components?
    >Components are reusable pieces of UI. Instead of writing everything in one file,
    >We break the Ul into small, manageable parts.
    >Components are reusable and they have different properties in each components.
Why Components?
                +----------------------------------+
                |             Header               |
                +----------------------------------+
                +-----------+----------------------+
                |           |                      |
                |  Sidebar  |    Main Content      |
                |           |                      |
                +-----------+----------------------+
                +----------------------------------+
                |             Footer               |
                +----------------------------------+
Why components?
    >Reusability
    >Better Organization
    >Eady to maintain
    >Scalability
Advantage of React Component
    • Reusability
        You can write a component once and reuse it wherever needed.
    • Maintainability
        Components are isolated — changes in one don't break others.
    • Separation of Concerns
        Each component handles its own UI, logic, and style.
    • Better Performance with Virtual DOM
        React only re-renders the components that change.
    • Composition
        Components can be nested inside others to build complex UIs.
    • Easy to Debug & Understand
        Smaller components make code easier to test and maintain.
Types of Components?
    >React components come in two types:
        1.functional component
        2.Class components  

Class Component:
    <>CLass component are ES6 classes that extend the React.Component class.
    <>They define a render() method to returns JSX, along with additional feature like state management and lifecycle methods.    

Difference Class and Functional Components

------------------------------------------------------------
| Functional Components     | Class Components             |
------------------------------------------------------------
| Functional Component is   | Class component is complex   |
| easy to create            | to create                    |
------------------------------------------------------------
| Functional component are  | Class component little slow  |
| fast in rendering         | in rendering                 |
------------------------------------------------------------
| In functional component   | In class component state     |
| we can assign any data    | should be assigned with      |
| type value to the state   | object only                  |
------------------------------------------------------------
| Hooks can be used         | Hooks cannot be used in      |
|                           | class component              |
------------------------------------------------------------

Props intro:
    <>props stands for properties
    <>props are used to pass date from one component to another component in React.
They make Components:
    <>reusable
    <>dynamic
    <>configurable

MODULE-4
---------
What are props?

    • Props stand for "Properties." They are read-only components. It is an object which stores the value of attributes of a tag and work similar to the HTML attributes.
    • Props are immutable so we cannot modify the props from inside the component.
    • Functional components receive props as parameters
    • Props play a crucial role in creating dynamic and reusable components by allowing them to be configured with different data based on their usage within the application.

How Props Work?
    Parent component sends data as props.
    Child component receives props as an argument.
    We use props.name, props.age to access the data.
    Pro tip: use meaningful prop names it makes your code
    easier to read and understand.
Prop Destructuring:
    <>prop destructuring in React functional components is a common patterns that allows you to extract values directly from the props object,marking your code cleaner and easier to read.

Default Props:
    <>It is necessary to always add props in the reactDom.render() element. you can also set default props directly.
react state 
    > state is used to store and manage data inside a component.
    > when state change the UI update automatically.
why state?
    > make component interactive
    > UI updates based on data Changes
    > handles dynamic data(forms,counter  ,taggles,etc)
    >improves user experience.

useState() Hook
    •⁠  ⁠The useState hook is a built-in React hook that allows functional components to manage stateful data without using class components.
    •⁠  ⁠It returns a pair of values: the current state and a function to update that state. useState enables functional components to have their own state, making them more powerful and flexible.

Syntax:
    const [state, setState] useState(value for variable)

What is useState() ?
    •⁠  ⁠useState() is simple predefined javascript function which is coming from react library.
    •⁠  ⁠useState will return one array which contains exactly two elements ie: state, setState.
    •⁠  ⁠First element is a state variable and second element is a function(setter function).
What is State?
    - State is a special variable in react where we can store any type of data.
    Example:
        let[state,setState]=useState(o)
        let[state.setState]=useState("Virat")
        let[state.setState]=useState((name:"raj",age:27])
        let[state.setState]=useState(true)
        let[state.setState]=useState([10,20,30,50])