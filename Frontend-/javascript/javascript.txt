===> javascript <===
> it is a scripting and prrogramming language used to add functionality to the webpage.
> 95% webpage are desined using javascript javascript
> it is ligth weigth because it will not consume much memory
> cross platfom:
        user can write code in one device and use can run in anoter diveics
> javascript it is dynamically type longuage ()
        in js while declareing variables no data type name so iti called dynamically type
> no need of any complier install it comple an browser it has built in complier

===> what is                                                                                                                                                                                                  javascript<===
> Javascript is a light weight object-oriented programming language used for client and sever side technology
> It was called scripting language before 2010 because it as used only for browser
> But now we can use this language for database server, application server and mobile devices also so it is called programming

===> Role of JavaScript in web development <===
> JavaScript is the programming language that makes websites Interactive, dynamic, and functional.
> if you think of a website like a human body:
        * HTML Skeleton (structure)
        * CSS→ Appearance (design)
        * JavaScript→ Brain (behavior & logic)
Without JavaScript, websites would be mostly static pages
===> history of javascript <===
> In 1993 mosaic was the first popular web browser to handle (GUI) but it could handle only static webpages
> In 1994 Netscape corp started new project called Netscape navigator
> Netscape navigator is a browser which have dynamic capabilities.
> It was invented by Brendan Eich. He was given the task to create programming language which would add dynamic capabilities to browser.
> It has been officially released on 1995 as name called mocha. later they give name livescript.
> Due to marketing purpose they give name javascript. Because java was more popular during 1990's
===> featuer of javascript <===
> All popular web browsers support javascript as they provide built in execution environments.
> Js follows the C language syntax and structure so it is called structured programming language.
> Js is an object oriented programming language like it has oops concept.
> It is a lightweight and interpreted language.
> It is a case sensitive language.
> Js is supported in servaral operating systems including windows, macos, linux etc.
===> Uses of JavaScript <===
> Used for building simple and complex web applications. example: google maps
> We can create mobile apps using javascript. (React Native)
> Game development (physics.js,pii.js)
> Creating webservers (node js)
> Creating client side applications or dynamic user interface (React is)
===> What is the purpose of JavaScript? <===
> It is used to create dynamic user interface on webpage.
> It is used to do cifent side validation and server side validation 
> It is used to interact or integrate front end app and backend application
===> What kind of application we can build using JavaScript? <===
> We can build entire web application
> We can build mobile apps like jos, android.
> We can develop standalone application (desktop application liks vscode, notepad, browser, ms paint etc)
> We can build machine learning application
            JavaScript                           Java
---------------------------------------------------------------------------
Origin      * Created in 1995 for                * Created in 1995 by
              web browsers                          Sun Microsystems
                                                    (now owned by Oracle)

Type        * Scripting / Programming             * Pure Programming
              language                              language

Execution   * Runs in browser and                 * Runs on Java Virtual
              Node.js                               Machine (JVM)

Platform    * Cross-platform                      * Platform-independent
                                                    (Write once, run anywhere)

Compilation * Interpreted language                * Compiled language
              (runs directly in browser)            (needs compiler)

Usage       * Mainly used for web                 * Used for enterprise
              development                           applications, Android apps

Speed       * Faster for web pages                * Faster for large
                                                    applications

File        * .js extension                       * .java extension

Syntax      * Simple and flexible                 * Strict and structured

Typing      * Dynamically typed                   * Statically typed
              (no data type required)               (data type required)

Example     * var x = 10;                         * int x = 10;

OOP         * Supports Object-Oriented            * Fully Object-Oriented
              Programming concepts

Memory      * Lightweight                         * Uses more memory
===>Where Does JavaScript Run? <===
> JavaScript runs in browser and node js
    In Web Browsers (Frontend)
> Every modern browser has a built-in JavaScript engine.
> Examples:
    * Google Chrome V8 Engine
    * Mozilla Firefox→ Spider Monkey
    * Microsoft Edge V8 Engine
    * Safari→ JavaScriptCore
===>javascript ways <===
> we write the javascript in three way there are :
        * head section -> write code inside  the head section
        * body section -> write code inside the body section it best to write a code
        * external js  -> write code in another files like : indedx.js
===>javascript data type<==
        •	 Javascript Datatypes
- It is used to define the type of data which we are adding to varlables
       -  There are 8 types datatypes in javascript
1. Number
2 string
3.boolcan
4. undefined
5.nulll
6. Bigint
7 symbol
B.Object type
* primitive data types
1. Number
2. string
3.boolean
4. undefined
5.null
6.Bigint
7. symbol.
all primitive datatypes are basic datatypa all primitive datatypes are inutable stored directly in stack memory

* non primitive data types
1. objest
aon primitira datatypes are copler
non primitive datatypes are stable stored directly in heap mendry
but array is net catatype it valll comes uder coject catatya
==================================================================================================================================================
                JAVASCRIPT

======================================== 
JAVASCRIPT FEATURES
========================================

1.  Lightweight JavaScript uses less memory and runs quickly in the
    browser.

2.  Interpreted Language JavaScript does not need a separate compiler.
    The browser reads and executes the code line by line.

3.  Dynamically Typed You do not need to specify the data type.

Example: let x = 10; x = “Hello”;

4.  Cross Platform JavaScript code can run on:

-   Windows
-   Mac
-   Linux
-   Mobile devices

5.  Object-Oriented JavaScript supports OOP concepts like:

-   Objects
-   Classes
-   Inheritance
-   Encapsulation

======================================== 
 VARIABLES 
========================================

A variable is a named memory location used to store data.

Syntax: keyword variableName = value;

Example: let age = 21;

Rules for Variable Names:

1.  Must start with:
    -   Letter
    -   Underscore (_)
    -   Dollar ($)
2.  Cannot start with number

Wrong: let 1age = 21;

3.  Cannot use keywords

Wrong: let let = 10;

4.  Case sensitive

age and Age are different.

----------------------------------------
tpye 1: var 
----------------------------------------
var is the old way to declare variables.
Example: var name = “John”;
Characteristics:

-   Function scope
-   Can redeclare variable
-   Can update value
-   Hoisted to top

Example:

var x = 10; var x = 20;

----------------------------------------
type 2: let 
----------------------------------------

let is modern variable declaration.

Example: let city = “Mumbai”;

Characteristics:

-   Block scope
-   Cannot redeclare
-   Can update value
-   Recommended to use

Example:

let age = 25; age = 30;
----------------------------------------
type 3: const 
----------------------------------------

const is used for constant values.

Example: const PI = 3.14;

Characteristics:

-   Block scope
-   Cannot update value
-   Cannot redeclare
-   Must assign value immediately

Wrong:

const x;

======================================== 
DATA TYPES
========================================

JavaScript has two categories of data types:

1.  Primitive Data Types
2.  Non-Primitive Data Types

  ----------------------
  Primitive Data Types
  ----------------------

1.  String
Stores text.
Example: let name = “Rahul”;

2.  Number
Stores numbers.
Example: let marks = 95;

3.  Boolean
Stores true or false.
Example: let isPassed = true;

4.  Undefined
Variable declared but not assigned.
Example: let x;

5.  Null
Represents empty value.
Example: let data = null;

6.  BigInt
Used for very large numbers.
Example: let bigNumber = 12345678901234567890n;

7.  Symbol
Used to create unique identifiers.
Example: let id = Symbol(“user”);

  --------------------------
  Non-Primitive Data Types
  --------------------------
1.  Object
Stores key-value pairs.
Example:
let person = { name: “John”, age: 25 };

2.  Array
Stores multiple values.
Example:
let numbers = [10, 20, 30];

3.  Function
A block of reusable code.
Example:
function greet() { console.log(“Hello”); }

======================================== 
 TYPEOF OPERATOR 
========================================

typeof is used to check the data type of a variable.

Example:

let age = 25; console.log(typeof age);
Output: number

More Examples:
typeof “Hello” → string typeof true → boolean typeof null → object
typeof [] → object

========================================
 TYPE CONVERSION 
========================================
Type conversion means changing one data type into another.
  ------------------------
  1. Implicit Conversion
  ------------------------
JavaScript automatically converts data types.

Example:
let result = “5” + 2;
Output: 52

  ------------------------
  2. Explicit Conversion
  ------------------------
Programmer manually converts data type.

String to Number:
let num = Number(“100”);

Number to String:
let str = String(100);

Boolean to Number:
let num = Number(true);
Output: 1
========================================
Operators in javascript
========================================

- Javascript operators are symbols that are used to perform operations on operands
- We have different types of operators they are
      1. Arithmetic operators
      2. Comparison operators
      3. Bitwise operators
      4. Logical operators
      5. Assignment operators
      6. Special operators
      
-------------------------
  1. Arithmetic operators
-------------------------

Definition:
Arithmetic operators are used to perform mathematical calculations.

---------------------------------
| Operator | Meaning            |
|----------|--------------------|
| +        | Addition           |
| -        | Subtraction        |
| *        | Multiplication     |
| /        | Division           |
| %        | Modulus (remainder)|
| **       | Exponent (power)   |
| ++       | Increment          |
| --       | Decrement          |
---------------------------------
--------------------------
  2. Comparison operators
--------------------------

Definition:
Comparison operators compare two values and return true or false.

------------------------------------------
| Operator | Meaning                     |
|----------|-----------------------------|
| ==       | Equal to (value only)       |
| ===      | Strict equal (value + type) |
| !=       | Not equal                   |
| !==      | Strict not equal            |
| >        | Greater than                |
| >=       | Greater than or equal       |
| <        | Less than                   |
| <=       | Less than or equal          |
------------------------------------------
------------------------
  3. Bitwise operators
------------------------

Definition:
Bitwise operators work on binary numbers (bits).

-----------------------------------
| Operator | Meaning              |
|----------|----------------------|
| &        | AND                  |
| |        | OR                   |
| ^        | XOR                  |
| ~        | NOT                  |
| <<       | Left shift           |
| >>       | Right shift          |
-----------------------------------
------------------------
  4. Logical operators
------------------------

Definition:
Logical operators are used to combine conditions.

---------------------
| Operator | Meaning|
|----------|--------|
| &&       | AND    |
| ||       | OR     |
| !        | NOT    |
---------------------
--------------------------
  5. Assignment operators
--------------------------

Definition:
Assignment operators assign values to variables.

-------------------------------------
| Operator | Meaning                |
|----------|------------------------|
| =        | Assign value           |
| +=       | Add and assign         |
| -=       | Subtract and assign    |
| *=       | Multiply and assign    |
| /=       | Divide and assign      |
| %=       | Modulus and assign     |
| **=      | Power and assign       |
-------------------------------------
------------------------
    6. Special operators
------------------------

Definition:
Special operators perform special operations like checking type or object properties.

-------------------------------------------
| Operator   | Meaning                    |
|------------|----------------------------|
|(?:).       |conditional operater        |
| typeof     | Check data type            |
| instanceof | Check object type          |
| in         | Check property in object   |
| delete     | Remove property            |
| new        | Create new object          |
-------------------------------------------
========================================
        JavaScript Function
========================================

Definition:
A function is a block of code designed to perform a specific task.
A function is executed when it is called (invoked).

Advatages of function:
    1.recusability of code
    2. less coding
How to create a function in js?
    *Always the function name should represent the particular behavior.
    *function won't get automatitally invoked unless and until we invoke.
Syntax:

function functionName(parameters) {
    // code to be executed
}
-------------------------------
    Type of js function
-------------------------------
  1 named function
  2.⁠ ⁠parameterized function
  3.⁠ ⁠return function
  4 anonyinous function
  5.⁠ ⁠callback function
  6.Hiigher order function
  7.arrow function
  8.immediately invoked function
  9.⁠ ⁠Currying function
  10.Pure function
  11.async function

Named Function:
----------------
Definition:
A named function is a function defined using the function keyword
with a specific name.

Advantages of Named Function:
    1. Easy to reuse the function.
    2. Improves code readability.
    3. Can be called multiple times.

How to create a Named function in JS?
    * Use the function keyword.
    * Provide a function name.
    * Call the function to execute.

Syntax:
function functionName() {
    // code to be executed
}


Parameterized Function:
-----------------------
Definition:
A parameterized function is a function that accepts input values called parameters.
Advantages of function:
    1.Dynamic input handling
    2.Code flexibility
How to create a function in JS?
    * Define parameters inside parentheses
    * Pass values while calling the function
Syntax:
    function functionName(parameter1, parameter2) {
        // code
    }

Return funciton:
-----------------
Definition:
A return function is a function that sends a value back using the return keyword.
Advantages of function:
    1. Returns result to caller
    2. Can store output in variables

How to create a function in JS?
    * Use return keyword
    * Store the returned value
    * ofter using return it belowe code is not run 

Syntax:
    function functionName() {
        return value
    }

Anonymous Function:
---------------------
Definition:
An anonymous function is a function without a name.

Advantages of function:
    1. Useful for temporary operations
    2. Used in callbacks and events
How to create a function in JS?
    * Assign function to a variable
Syntax:
    var variableName = function() {
          // code
    }

Callback Function:
---------------------
Definition:
A callback function is a function passed as an argument to another function.

Advantages of function:
    1. Enables asynchronous programming
    2. Improves flexibility
How to create a function in JS?
    * Pass a function as parameter
Syntax:
    function mainFunction(callback) {
        callback()
    }

Arrow Function:
----------------
Definition:
An arrow function is a shorter syntax for writing functions using =>.
Advantages of function:
    1. Shorter syntax
    2.Cleaner code
How to create a function in JS?
    * Use arrow symbol =>
Syntax:
    const functionName = (parameters) => {
        // code
    }

Immediately Invoked Function (IIFE):
--------------------------------------
Definition:
An IIFE is a function that runs immediately after it is defined.
Advantages of function:
    1. Runs automatically
    2. Avoids global variable pollution
How to create a function in JS?
    * Wrap function in parentheses
    * Add () to invoke
Syntax:
    (function() {
        // code
    })();

Currying Function:
---------------------
Definition:
A currying function is a function that returns another function.
Advantages of function:
    1. Breaks complex functions
    2. Improves reusability
How to create a function in JS?
    * Return a function inside another function
Syntax:
    function functionName(a) {
        return function(b) {
            // code
        }
    }

Pure Function:
-----------------
Definition:
A pure function always returns the same output for the same input and has no side effects.
Advantages of function:
    1. Predictable results
    2. Easy testing
How to create a function in JS?
    * Use only input parameters
    * Do not modify external variables
Syntax:
    function functionName(parameters) {
        return result
    }

Async Function:
------------------
Definition:
An async function is a function that works asynchronously using async and await.
Advantages of function:
    1. Handles asynchronous operations
    2. Improves readability of async code
How to create a function in JS?
    * Use async keyword
Syntax:
    async function functionName() {
        // code
    }                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 
============================
JAVASCRIPT OBJECT — THEORY
============================
• JavaScript is an object-based language.
• An object is a combination of properties and behavior (methods).
• An object represents a real-world entity.
• JavaScript is template-based, not purely class-based.
• An object is a collection of key and value pairs.

Example:
name → "John"
age → 25

Here:
name and age are keys
"John" and 25 are values


HOW TO CREATE OBJECTS IN JAVASCRIPT

1. By using Object Literals
2. By using Constructor Function
3. By using Object.create()
4. By using Class Syntax (ES6)


OBJECT OPERATIONS (CRUD)

1. Create / Insert
   object.key = value
   object["key"] = value

2. Read
   console.log(object.key)

3. Update
   object.key = newValue

4. Delete
   delete object.key


OBJECT METHODS

Object.keys()     → returns all keys
Object.values()   → returns all values
Object.entries()  → returns key-value pairs


REAL-WORLD EXAMPLE

Student Object:
Name
Grade
Study Method

Product Object:
Name
Price
Stock
Category
Rating

 OBJECT CREATION 
   - An object is created using {}.
   - It contains key-value pairs.
   - Example:
       name: "santhu"
       age: 21
 PROPERTIES OF OBJECT
   - Properties are data stored inside an object.
   - Example:
       name, age, height, city

ARRAY INSIDE OBJECT
   - An object can contain arrays as values.
   - Example:
       skill: ["sleeping", "eating", "sleeping"]

METHODS IN OBJECT
   - A function inside an object is called a method.
   - Example:
       play: function(){...}

"this" KEYWORD
   - Refers to the current object.
   - Used to access object properties inside methods.
   - Example:
       this.name
       this.height
 ACCESSING OBJECT DATA
   - Dot notation:
       person.age
   - Array index:
       person.skill[1]

DISPLAYING OBJECT
   - console.log(person)
   - Prints full object

OBJECT METHODS

   a) Object.keys(object)
      - Returns all keys in an array
      - Example output:
        ["name","age","skill","height","city","play"]

   b) Object.values(object)
      - Returns all values in an array
      - Example output:
        ["tanu",21,[...],"150cm","matikere",function]

OBJECT.SEAL()
----------------
   - Prevents adding or deleting properties
   - Existing properties CAN be modified
   - Example:
       person.newProp = "test"  ❌ Not allowed
       delete person.age       ❌ Not allowed
       person.age = 22         ✅ Allowed

note:-
   - Object stores data + behavior
   - "this" refers to current object
   - Object.keys() → get keys
   - Object.values() → get values
   - Object.seal() → restrict structure
   
Object.assign()
-----------------
- Object.assign() is a built-in JavaScript method.
- It is used to copy properties from one or more source objects to a target object.
- It merges objects together into a single object.
- It returns the modified target object.

MAIN PURPOSE

1. Copy properties from one object to another
2. Merge multiple objects
3. Create a shallow copy of an object

IMPORTANT POINTS

- It copies only enumerable properties.
- It performs a shallow copy (not deep copy).
- If properties have the same name, the last value overwrites the previous one.
- The target object gets updated.

SYNTAX

Object.assign(target, source1, source2, ...)

TERMS

Target:
The object that receives the properties.

Source:
The object(s) from which properties are copied.

Object.entries()
---------------------
- Object.entries() is a built-in JavaScript method.
- It returns all the properties of an object.
- The result is returned as an array.
- Each element in the array is a pair of key and value.
- The pair is stored as a sub-array.

PURPOSE

- To get both keys and values together.
- To convert an object into an array format.
- Useful for looping through object data.

RETURN TYPE

- Returns an array of key-value pairs.

SYNTAX

Object.entries(object_name)

IMPORTANT POINTS

- It includes only enumerable properties.
- The order of entries follows the order of properties in the object.
- It does not change the original object.

Object.freeze()
--------------------
- Object.freeze() is a built-in JavaScript method.
- It is used to completely lock an object.
- It prevents adding new properties.
- It prevents deleting existing properties.
- It prevents modifying existing property values.
- The object becomes read-only.

PURPOSE

- To protect an object from any changes.
- To make object data constant.
- To ensure data safety.

SYNTAX

Object.freeze(object_name)

IMPORTANT POINTS

- No new properties can be added.
- Existing properties cannot be deleted.
- Existing property values cannot be changed.
- It returns the same frozen object.
- It performs a shallow freeze (nested objects can still change).

=========================
JAVASCRIPT ARRAY
=========================
- JavaScript Array is a special variable used to store multiple values in a single variable.
- Arrays store data in an ordered list.
- Each value in an array is called an element.
- Each element has an index number.
- Index numbering starts from 0.

CHARACTERISTICS OF ARRAY

1. Arrays can store multiple values.
2. Arrays can store different data types.
3. Arrays are dynamic (size can change).
4. Arrays are zero-indexed.
5. Arrays are objects in JavaScript.

ARRAY CREATION METHODS

1. Using Array Literal
2. Using new Array() constructor

ARRAY SYNTAX
-------------------------

1. Array Declaration

   var array_name;

2. Array Initialization (Array Literal)

   var array_name = [value1, value2, value3];

3. Array Using Constructor

   var array_name = new Array(value1, value2, value3);

4. Accessing Array Element

   array_name[index];

5. Updating Array Element

   array_name[index] = new_value;

6. Finding Array Length

   array_name.length;

ARRAY INDEX

- Index is the position of an element in an array.
- First element index is 0.
- Second element index is 1.
- Last element index is length - 1.

ARRAY PROPERTIES

length
- Returns the number of elements in the array.

ARRAY COMMON OPERATIONS

1. Add element
2. Remove element
3. Access element
4. Update element
5. Traverse element

COMMON ARRAY METHODS

push()
- Adds element at the end of array.
- when we need to push any data to na array we use push method .push() always appends the ena of the array.

pop()
- Removes element from the end.
- array.pop()

shift()
- Removes element from the beginning.
- array.shift()

unshift()
- Adds element at the beginning.
- when we need to push rhe data to the beginningof the array.we use the .unshift()

slice()
- Returns a portion of the array.

splice()
- Adds or removes elements from array.

indexOf()
- Returns index of an element.

includes()
- Checks if element exists.

join()
- Converts array to string.

reverse()
- Reverses the array.

sort()
- Sorts the array elements.
-let variable=array_name.sort() or let variable =array_name.sort(()=>{})

forEach()
- Executes function for each element.
- if we want to access each and every element of an array we are using forEach() .forEach() will not return any value it will not alter original array
- array_name.forEach((element,index){console.log(element +" "+index)})

map()
- Creates new array after applying function.
- map() should be return some value returned value always added in new array map() will not after original array
- let variable = array_name.(call back)

filter()
- Returns elements that match condition.
- it is used to filter element or data from the array based on certain condition filter callback function will return boolean value it will not alter original array it will create new array
- let variable= array_name.filter(function(element,index){return <boolean>})

reduce()
- Reduces array to a single value.
- it is used to reduce the array to one singl value reduce method is such a method which accepts a callback function as parameter (first parameter)(second parameter)initial value
- let variable =array_name.reduce(call back,initial value)

slice()
- Whenever we need to extract particular element from array we can use slice() it will not alter the original array.
- slice(startindex, endindex)

splice()
- Extraction start at the start index extract the length number of  element from the array in case splice() it also alter the original array
- splice(start,length)

toStrig()
- the toString () return a string with array values separate by commas
- array.toString()

delete()
- array element can be deleted using javascript operater delete using delete leves undefined holes in the array 
- delete.array_name(variable_name) 

isArray()
- this isArray() method return true if an variable storing an array otherwise false
- array.isArray(variable_name)
=========================
JAVASCRIPT STRING
=========================
<--Java Scripts Strings-->
>collection of characters enclosed with in quotes(single/double) is considered to be string

<--String Concatenation-->
>combining one or more strings together is called as concatenation in order to cocat 2 strings we should take the help of + operators.
 var firstname="virat"
 var lastname="kholi"
 console.log(firstname+" "+lastname)

 <--How to find the length of a given string?
 >to find the length of a given strings we use length property
 let city="Bangalore"
 console.log(city.length)

 <--String Methods-->
 1.charAt()
 2.toLowerCase()
 3.toUpperCase()
 4.slice()
 5.substring()
 6.replace()
 7.replaceAll()
 8.trim()
 9.concat()

1.charAt():
.whenever from a string if we need to extract a specific character we use charAt()
var str="hello world"
console.log(str.charAt(4))

2.toLowerCase():
.all the characters of the string will be converted to lower case.
    1.let str="HELLO WORLD"
    2.console.log(str.toLowerCase())

3.toUpperCase():
.all the characters of the string will be converted to upper case.
    var str-"hello world"
    console.log(str.toUpperCase())

4.substring():
.its almost similar to slice(). the difference between substring and slice() is substring doesn't support -ve indedxes.
<--SELECTION OR CONTROL STATEMENT-->
<>we can control the execution of the program using control or selection statements. 
we can execute certain set of statements only if certain condition become true.

<--types-->
1.if
2.if else 
3.else if
4.switch
5.ternary operator

1.if:
syntax:
  if(condition){
    //set of code
  }

2.if else:
syntax:
  if(condition){
    //set of statements
  }
else{
  //set of statements
}

3.else if:
syntax:
  if(condition){

  }
  else if(condition){

  }
  else{

  }

4.switch:
syntax:
  switch(){
    case"value":{
      //set of statements
      //break;
    }
    case :value":{
      //set of statements
      //break;
    }
    default:{
      //set of statements
    }
  }

5.ternary operator:
syntax:
condition?//true statement: //false statement

<--LOOPS-->
<>loops are used to complete repetitive tasks easily instead of manually repeating the tasks we must take advantage of loops.

<--types-->
1.for loop
2.while loop
3.do while loop
4.for in loop
5.for of loop

1.for loop:
syntax:
for(variable integer;condition;increment/decrement)
{
  //sey of statements
}

2.while loop:
syntax:
while(condition){
  //set of statements increment/decrement
}

3.do while loop:
syntax:
do{
  //set of statements increment/decrement
}
while(condition)

4.for in loop:
syntax:
for(variable in object_name){
  //set of statements
}

5.for of loop:
syntax:
for(variable of arrguments)
{
  //set of statements
}
<--nan (not a number) operators-->
<>in java is a special value used in programming and mathematics to represent a value that is undefined or cannot be represented as a valid number.

What does NAN occur?
<>when you perform mathematical operations that doesn't return a valid number.
to check the given value is nan or not

isNAN():
<>it tries to convert the value to number before checking

number.is.NAN():
<>checks strictly if the value is actually NaN value
Ex:
isNAN("abc")
number.isNan

<---DOM(document object model)--->
<>dom is the interface between javascript and browser javascript in order to communicate with the browser takes help of dom
<>dom is an tree like structure

main uses of dom:
1.allow us to make js interact with browser
2.we can write js to create, modify and delete html elements 3.we can dynamically add styles, classes and attributes to the html elements
4.dom is very complex api(application programming interface) which contains lots or methods and properties which help us to interact with browser

why dom is created?
<> using javascript we can manipulate the html document so that we can create dynamic user interface

dom manipulation:
<>how to access dom node (html element)
<>how to add new html element
<>how to remove existing html element
<>how to change content or atributes of html elements
<> to perform above operation browser is providing us predefined objects and functions called dom api

Dom methods:
1.document.write()
2.document.head
3.document.body
4.document.URL
5.document.getElmentByld()
6.document.getElementsByClassName()
7.document.getElementsByTagName()
8.document.getElementsByName()
9.document.querySelector()
10.document.querySelectorAll()
11.document.createElement()
12.remove()
13.getAttribute()
14.setAttribute()
15.document.title

1.document.write():
<>it helps us write some text directly to the html document.
        document.write("hii hello good morning")

2.document.head:
<>it helps to access enitre head section of the html document 
        var head=document.head
        console.log(head)

3.document.body:
<>it helps to access enitre body section of the html document
        var body=document.body 
        console.log(body)

4.document.url:
<>it helps to access URL address of the html document
        var url=document.URL
        console.log(url)

5.document.title:
<>it helps to access the title of the webpage
        var title=document.title
        console.log(title)

6.document.getElementById():
<>its used to find the elements on the webpage based on the id attribute. it will return single element which matches with id value.
ex:
<h1 id="one">welcome to pentagonspace</h1> 
<p id="two*></p>
<button onclick="Change()">Click here</button>
function Change(){ 
    var hiref=document.getElementById('one') 
    hiref.innerHTMLa"Thank you for visting
}
    var prefadocument.getElementayId('two') 
    pref.textContent="welcome to javascript class
    
Difference b/w textContent and innerHTML

innerHTML
if we have any html tags present while using inner html then inner html property recognizes the html tags.
    var h2ref=document.getElementById('three') 
    h2ref.innerHTML="<i>hello good morning</i>"

textContent
if we have any html tags present while using textContent will not recognises and it will print html tags as it is
    var p1ref=document.getElementById('four') 
    piref.textContent="<i>hello good morning</i>"

7.document.getElementsByName():
<>its used to find the elements on the webpage based on the name attribute. it returns nodelist it is nothing but array of node or html elements
ex:
        <label>username</label><input type="text" name="username" >
        <label>password</label><input type="text" name="password">
    <br><br>
    <label>username</label>cinput types"text" namen"username"> 
    <label>password</label><input type="text" typen"text" name="password">
    <h1>pentagonspace</h1>
    <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Libero, quod ad

        var username=document.getElementsByName('username') 
        console.log(username)
        var password=document.getElementsByName('password')
        console.log(password)

8.document.getElementsByTagName():
<>this method used to select all elements with a specified tag name from the document.
ex:
    <label>username</label><input type="text" name="username">
    <label>password</label><input type="text" name="password">
    <br><br>
    <label>username</label><input type="text" name="username">
    <label>password</label><input type="text" name="password">
    <button onclick="change()">submit</button>
    <h1>Dhee coding lab</h1>
    <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Rerum doloremque sapiente quisquam odio, eius corrupti exercitationem magnam illo libero, 
        rem earum sed quod vitae ea voluptatem? Nesciunt nulla aperiam animi!</p>
    <script>
    let labelref=document.getElementsByTagName("label")
    console.log(labelref)
    labelref[0].innerHTML="Email"
    labelref[2].textContent="Email"
    labelref[1].style.color="green"
    labelref[1].style.textDecoration="underline wavy red"

    let inputref=document.getElementByTagName("input")
    console.log(inputref)
    for(var data of inputref){
        data.style.border="5px solid blue"
    }
    let h1ref=document.getElementsByTagName("h1")
    console.log(h1ref)
    h1ref[0].style.color="orange"
    </script>
    
9.document.querySelector():
<>query selector() returns the first element in the document that matches the specific selector or group of selector if no matches are found then it will return null.
<>As a query we can pass class name, ID name, and tagname
ex:
    <h1>pentagonspace</h1>
    <p>Lorem ipsum dolor sit amet consectetur adi
    <h1 >hello everyone</h1>
    <h4 class="four">this is the heading</h4>
    <h4 class="four">this is h4</h4>
    <p id="one">Lorem ipsum, dolor sit amet conse
    <p id="one">Lorem ipsum dolor sit amet consec

    var res-document.querySelector('h1')
    console.log(res)
    var res1-document.querySelector('p)
    console.log(res1)
    var res2-document.querySelector(#one)
    console.log(res2)
    var res3-document.querySelector(. four)
    console.log(res3)

10.document.querySelectorAll():
<>it returns all the elements which matches the query as an array.
ex:
    <h1>pentagonspace</h1>
    <p>Lorem ipsum dolor sit amet consectetur adi
    <h1 >hello everyone</h1>
    <h4 class="four">this is the heading</h4>
    <h4 class="four">this is h4</h4>
    <p id="one">Lorem ipsum, dolor sit amet conse
    <p id="one">Lorem ipsum dolor sit amet consec

        var result=document.querySelectorAll('p') 
        console.log(result) 
        var result1=document.querySelectorAll('#one') 
        console.log(result1)

11.document.createElement():
<>we can create web elements dynamically using createElement() in order to dynamically append created element to webpage we can take the help of appendChild()
ex:
    var body=document.getElementById('body')
    var para=document.createElement('p') 
    para.innerHTML="i am paragraph created dynamically" 
    body.appendChild(para)
    
    var button=document.createElement('button') 
    button.textContent="dynamic button" 
    body.appendChild (button)

12.remove():
<>by using remove() we can remove web elements dynamically from html document
ex:
    <body id="body"> 
    <p id="one">Lorem ipsum dolor sit am</p>

var pref=document.getElementById('one') 
pref.remove()

13.setAttribute():
<>the setAttribute(name, value) method used to set a new attribute or change the attribute value of exisitng attribute on an html element
ex:
    <input id="input">
    <input type="radio" id="one">option1 
    <img id="two" src="https://images.hdqwalls.com/

    let inputref=document.getElementById('input') 
    inputref.setAttribute('type, "number") 
    let inputref1-document.getElementById('one') 
    inputref1.setAttribute('type', 'checkbox')

    let aref-document.createflement('a')
    aref. innerHTML="Youtube
    aref.setAttribute('href, "https://ww.youtube.com")
    let body document.body
    body.appendChild(aref)

14.getAttribute():
<>the getAttribute(name) method retrieves the value of a specified attribute from an html element
ex:
    <input id="input">
    <input type="radio" id="one">option1 
    <img id="two" src="https://images.hdqwalls.com/

    let ref1=document.getElementById('one') 
    let res=ref1.getAttribute('type') console.log(res)
    let imgref=document.getElementById('two') 
    let result= imgref.getAttribute(src') console.log(result)
adding events through javascript
 > we can listen to events through Javascript by using addEventlistener()
 > addEventListener() accepts 2 parameters

 1.type of event(string) excclick change etc 
 2.caliback function

event delegation
  > event delegation is a technology in javascript where instead of adding event listener to individul child element , we can add a single event lister to a parent element
event bubbling
  >Event bubbling is a mechanism in JavaScript where an event starts from the deepest target element means the event trigger first on child element or clicked element and then moves upward to its parent elements
    >In event bubbling event direction will be from child to parent
>event capturing

    - The capturing phase is the first phase of event propagation in the DOM.
    - Event capturing means the event is triggered first of an outermost or parent element and then it goes to the innermost or child element.
    - To perform event capturing for the add event listener along with the event type and the callback function we have to pass a third argument as true

syntax:

    - element.addEventListener(event, function, true);
        or
    - element.addEventListener(event, function, (capture true));
    - event capturing direction will be from parent to child

Math object
    > math is an inbuilt object in javascript which can be uesd to perform some specific
    Math is an inbuilt object in JavaScript which can be used to perform
        console.log(Rath.PI)//print value of pi
        console.log(Math.sgrt (49))//print the square rost of mumber
        console.log(Math.pow (9,2))//print the power of number
        console log(Math.round(5.6))//round to nearest mumber
        console.log (Rath.floor(2.5))//floor it to nearest intager value
        console.log (Rath.trunc(239389.48727))//it will print intagar value
        //and remove the decimal value
        console.log(Math.xign(-93819))
        console.lng(Rath.sign(1773))//print the mumber is megative pr positive
        console.log (Rath.random())//print the random mumbers
Date Object

    When ever we need to know about the date we should use date object gives information regarding Day, Month, Year, Minutes, Hours Seconds, and Time Zone
    months->0->Jan 11->Dec
    week->0->Sunday 6->Saturday
    var date=new Date() console.log(date)

<>When ever we need to delay the execution or we need call function again and again we use timers.
Timers as two types they are:
1.setTimeout()
<>setTimeout() accepts 2 parameters
    1.callback function
    2.the time in milliseconds
2.setinterval():
<>setinterval() also accepts 2 parameters
    1.callback()
    2.the time in milliseconds

what is the difference between setTimeout() and setinterval()?
<>The main difference between the setTimeout() and setinterval() is that the setTimeout() will execute the callback() 
only once where as setinterval() keeps on triggering the callback() reguraly after the give interval of time (unless you stop it)

How to stop set interval()?
<>when we need to stop the setinterval() we should useclearinterval() every setinterval() will return one id we need that 
id in order to clear the interval clearinterval() accepts a number as a parameter.

 Template literals
    >Template literals (also called template strings) are a way to work with strings in JavaScript using backticks () instead of single (") or double
    ("")quotes.
    >They were introduced in ES6 (ECMAScript 2015)
    >String Interpolation: Embed expressions using ${}
    > Multi-line Strings: Write strings across multiple lines naturally
Spread operator
    >...<object names>
        OR
    >...<Array name>
    > It is used to copy properties of one object into another object and it is used to copy elements of one array into another array.
Destructing of array or object
    > it is technology in javascript to unpack properties or elements format object or array and assigning variable

Rest parameters
    >Rest parameter allow a function to accepts an indefinite number of arrguments as a arrayand also it can applied for array or object to store elements or properties
    syntax:

Hoisting
    • Holsiting is is default behavior of moving variable and function declarations to the top of their scope before execution
    • Works with var (hoisted but undefined) and function declarations (hoisted with definition)
    • let and const are hoisted but remain in the temporal dead zone
    • Both varibales and function are hoisted but behave differently
How it works internally?
    when JavaScript engine starts
        1.First phase(memory creation phase)
            • It scans the code
            • It creates memory space for variables and function variable declarations are set to undefined
            • Function declaration are fully hoisted (entire function definition)
        2.second phase
            • code runs line by line
Closures in JavaScript
    • It is a technique to access of outer function variable inside inner function
    • It is function to define function inside another function
    • A closures can be defined as a JavaScript feature in which the Inner function has access to the outer function varlable, Java Script, every time a dosure is created with the creation of function
The closures has three scope claims listed as follows
    1. access to Its own scope
    2 access to the variable of the outer function
    3. access to the global variables
    syntax:
        function outer(){
        //...................
        //...................
        function inner(){
        //...................
        }
        returng inner
        }
Scope in Javascript
    • scope refers to the accessibility, scope can be used for functions and variables.
    There are 3 scopes in JavaScript
        1 global scope
        2.function scope
        3. Lexical scope
    global scope
        • If any functions or variables or class not defined inside the block(0) then they are said to be in global scope
    function scope
        • Any thing which is defined inside function scope those thing we can access only inside the function scope
    Lexical scope
        •⁠  ⁠Lexical scope means that scope is determined by where variables and blocks are written in the code means inner scopes can access

This keyword
>in javascript this is a special keyword which refers to current object.
>this is a special keyword in JavaScript which will point to current object
>we can change reference of this keyword by using three predefined
functions they are.
    1.call()
    2.apply()
    3.bind()

<--Difference b/w call, apply, bind-->
1.call()
    calls a function immediately
    passes agruments one by one

2.apply()
    also calls a function immediately
    passes agruments array

3.bind()
    does not call function immediately 
    returns a new function with this bound 
    useful for creating a copy of the function 
    with fixed this and arguments

<--try/catch/finally-->
    >whenever we feel some part of code may cause problems we must enclose the part of code with in the try.
    >so if any problems occur try is designed in such a way that it throws the exception object outside which later will be caught by the catch() by using try-catch syntax we can avoid our programs getting terminated abruptly.

<--Deep Copy-->
    >A deep copy means creating a new completely independent copy of an object (or array), including all of its nested object/arrays.
    >In deep copy all the levels of the original object are copied the new object does not share references with the original changes made in the copy do not affect the original and vice versa.

<--shallow copy-->
    >a shallow copy is a copy of an object where only first level of the object is duplicated but the nested(inner) objects or references are not copied, they are just referenced.

Synchronous Programming
    • A synchronous function is one that runs in a single thread and completes its execution before moving on to the next task.
    • It will block other operations until the current task is completed.
    • synchronous programming means code execute line by line, that is one task at at a a time time and each line waits for the previous line to complete before executing.
Asynchronous Programming
    • Asynchronous programming in js is a non-blocking execution model where long-running tasks can be executed in the background without blocking the main thread. Means while other operations can continue to run.
    • Asynchronous programming is used with long-running tasks like network requests, file operations, timers, etc.
note:
• In js asynchronous programming execution take care by the event loop
• Event loop is part of the js runtime environment it is not part of is engine
• In is setTimeout and setinterval are the built-in functions that allow us to perform asynchronous operations.
• but we can also use Promises, async/await for better handling of async code.
• Asynchronous programming can be done by using timers, promises, async and await
Promise in JavaScript
    >These are the way to handle the asynchronous operations
    > A promise is a JavaScript object that links producing code and consuming
    code that represent a value may be available now or in the future or never
The promise object can be in any 3 states
            1.pending
            2.fulfilled
            3.reiected
promises
    |
-----------------------------------------
|                  |                   |
fulfilled        pending            rejected
|                                      |
v                                      v
resolve()                           reject()
|                                      |
v                                      v
then()                              catch()
Creating a Promise
// create a promise using promise constructor take a function with two parameter resolve and reject. Here the promise will be either be resolved or it will be rejected.

Handling promise result
// we can use .then() method to handle the promise result. If any error occurs, then we use .catch() method to handle the result.


Fetch()
fetch is a method which works internally as a promise. fetch method is used to make a network request or to call an API in JavaScript.
Fetch will return a promise in JavaScript.
async and await

I

•async:
    declare a function or method as asynchronous and can pause its execution to wait for completion of other process
•await:
    make a suspension point where execution may wait for the result of async function or methods
Event loop :
    event loop is a mechanism in javascript that takes from web api or task queue and pushes to call stack when it is empty 
    why we need event loop?
            • Is is single threaded language but some time it needs to perform a long running task like fetching data from server or reading a file system if js 
            would have performed these tasks on main thread it would have blocked the execution of other code so these task are taken care by event loop
        note:
            event loop is not part of js engine it is part of browser or node js runtime js engine it environme.
Acc JS Code
                              |
                           JS Engine
                              |
                            Parser
                              |
                         Interpreter
                              |
                        Compiler (JIT)

 -------------------------------------------------------------
|                                                             |
|                       JS Runtime                            |
|                                                             |
|   ------------------        -----------------------------   |
|  |    Memory Heap  |      |         Call Stack         |   |
|   ------------------        -----------------------------   |
|                                                             |
|                                                             |
|                         Web API                             |
|                                                             |
|   ------------------      ------------------------------    |
|  | Promise          |    | setTimeout()                |    |
|  | callback         |    | setInterval()               |    |
|  | async/await      |    | DOM events                  |    |
|   ------------------      ------------------------------    |
|                                                             |
|   Micro task queue         Macro task queue                 |
|                                                             |
|                         Event Loop                          |
|                                                             |
 -------------------------------------------------------------

                         Browser (Chrome)
flow of event loop
    1.so if a is task is a synchronous that uses only js engine and callback for execution
    2.so in js heavy tasks or asynchronous tasks goto web api through event loop 3. event loop checks if a call stack is empty or not
    4. if call stack is empty event loop pushes the microtask first to call stack for execution then runs macrotask next
    5.if call stack is not empty event loop waits till it is empty
    6.this process continues till all the tasks are executed.

microtask queue
    > micro task queue having higher priority in web api it hold the async task made by promises, callback, Async await and it always run before macrotask queue
macrotask queue
    > macro task queue having lower priority in web api it hold the async task made by setTimeout,setinterval,1/O operations and dom events it execute after microtask queue is empty

what is web api?
    >web api is a browser or nodejs provided api that helps js to perform
    > asynchronous tasks like setTimeout, fetch api, dom events etc
    > web api is not part of js engine it is part of browser or nodejs runtime environment
    > synchronous --> browser-->js engine-->call stack->execution
    > asynchronous ->browser->web apl-->event loop-->task queue-->call stack-->execution

callback hell
    >callback hell happens in js when you have multiple nested callbacks inside asynchronous function like setTimeout, api calls etc it makes code hared to read and hard to maintain
    >It is old pattern to handle asynchronous function in new patter we are using promises async/await method.
What is Web Storage?
    Web Storage allows browsers to store data on the client side (user's browser).

    There are 2 main types:
        >local Storage
        >session Storage
<--Local Storage-->
    >local storageis a type of browser storage that allows you to store key value pairs in the browser.
    >the data is stored even when the browser is closed and reopened.
    >share across tabs.

<--session storage-->
`    >session storage, on the other hands, is similar to local storage, but the data is only stored for the user's session.
    >stores data only for one browser session.
    >this means that the data is lost when the tab/windows is closed.
    >only current tab`